//Vanilla JS ONLY

//ADD - Create finish line

//ADD - Add event listeners two buttons
document.addEventListener('keyup', function(event) {
  const keyName = event.key;
  console.log(keyName + ' pressed')
});

//ADD - Move player
